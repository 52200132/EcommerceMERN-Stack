networks:
  app-network:
    driver: overlay

services:
  backend:
    image: phuocphung/backend_api:latest
    env_file:
      - ./backend/.env
    networks:
      - app-network
    ports:
      - "5000:5000"
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 30s
      update_config:
        parallelism: 1
        delay: 5s
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
  frontend:
    image: phuocphung/frontend_app:latest
    networks:
      - app-network
    ports:
      - "3000:80"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure